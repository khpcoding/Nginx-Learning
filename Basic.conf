server {
    listen 8080;  # Listen on port 80
    server_name localhost;  # Change to your domain name or IP address

    location / {
        default_type text/html;  # Set the response type to HTML
        return 200 "<html><body><h1>Service is Available</h1></body></html>";  # Return the message as HTML
    }
}
